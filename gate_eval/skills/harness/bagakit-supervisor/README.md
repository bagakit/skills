# Bagakit Supervisor Eval

This non-gating eval owner has two distinct surfaces:

- `suite.ts` and `cases/supervision-cases.json`
  - deterministic checker-policy coverage
  - proves receipt guards and explicit non-proof boundaries
  - structurally validates the separate semantic case bank without executing or
    scoring its hidden dispositions
- `protocol.md` and `cases/forward-cases.json`
  - preregistered live fault-injection comparison
  - currently `preregistered_unrun` and `blocked_not_runner_ready`
- `cases/outcome-ownership-cases.json`
  - fresh-context semantic cases for goal understanding before optimization,
    outcome ownership, assurance pipelining, goal-seeking initiative without
    activity bias, executor efficiency without micromanagement, reward-hacking
    resistance, evidence-directed review admission, execution-authored check
    self-proof, finding-driven Goal expansion, exact-current-candidate close,
    team-capacity admission, useful versus duplicate fan-out, and bounded
    Worker goal assimilation, multi-Agent convergence-frontier control,
    task-shaped quiet cadence, authorized shared-hotspot coordination,
    mixed-commit preservation, conflict-cone containment, plain-language
    precision, source attribution, event-driven Worker reporting,
    semantic-breakpoint user visibility, maximum-staleness fallback, truthful
    progress claims, compact-safe communication rebinding, and nonblocking
    delivery semantics
  - expected dispositions stay hidden from the Agent under test

The prior four-case live bundle is retained only as calibration evidence in
`cases/pilot-calibration.json`. It is an artifact-correctness smoke test whose
automated grader hit a complete ceiling; corrected blind artifact reviews were
not all at the ceiling. Neither surface is comparative Supervisor evidence.

## Claim Boundary

Do not claim frontier or general superiority from:

- deterministic receipt fixtures
- Agent-authored worker, route, or intervention fields
- one run per condition
- shared-workspace forward tests
- a repaired-in-place blind packet

The live protocol separates two questions:

1. On aligned work, does Supervisor avoid interference and bounded overhead?
2. On injected faults, does Supervisor prevent hard incidents with the smallest
   correction while preserving valid work?

The outcome-ownership cases add a third semantic question: does Supervisor
keep strict terminal proof while removing avoidable control-path delay, without
taking over Worker implementation or mistaking visible activity for progress?

Supervisor versus direct and Supervisor versus dispatch-only are separate
policy-bundle contrasts. The primary go/stop contrast is Supervisor versus
dispatch-only; neither isolates the causal effect of loading one skill.

## Current Next Gate

Fresh artifact review blocks runner freeze and live execution until all three
decision-bearing gaps are closed:

- treatment fidelity gate with host-observed model, capsule, skill, tool,
  routing, and dispatch-only delegation evidence
- executable typed truth rules for every per-case oracle
- reference recomputation of scores, identity bindings, aggregates, and the
  final decision receipt from sealed inputs

Before scored live runs, implement and verify:

- isolated run namespaces
- append-only host telemetry
- immutable hidden oracles and complete snapshot capture
- deterministic fault daemons
- sealed outcome and control packets
- per-cell grading that cannot abort the batch or retain stale scores
- a sealed candidate-input builder that consumes only the declared allowlist

The structural checker pins and structurally checks the current snapshot plus
calibration accounting only. Its 30-case self-test demonstrates canonical
digest sensitivity, not 30 independent semantic validators. Until the blockers
and execution surfaces above are resolved, pass 003 remains blocked, unrun,
not runner-ready, and provides no observed capability evidence.
