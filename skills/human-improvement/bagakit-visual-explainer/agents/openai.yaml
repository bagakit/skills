interface:
  display_name: "Visual Explainer"
  short_description: "Explain complex topics visually from zero context"
  default_prompt: "Use $bagakit-visual-explainer to introduce this topic from zero context as a low-text visual HTML artifact in my language, with an architecture map and causal depth."
