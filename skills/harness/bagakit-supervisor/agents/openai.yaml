interface:
  display_name: "Bagakit Supervisor"
  short_description: "Own delivery without taking implementation"
  default_prompt: "Use $bagakit-supervisor to own the delivery judgment and Writer team, synthesize Owner input before routing, preserve aligned Worker attention by default, and drive the current outcome to strict proof."
